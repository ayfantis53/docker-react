language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t ayfantis53/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ayfantis53/docker-react npm run test